# 本地开发（默认 npm run dev）
VITE_DEPLOY_ENV=development
# true：axios 直连 http://localhost:8889（需本地启动 manager-server）
VITE_API_LOCAL=true
# vite dev proxy 目标（/api 转发，可按需改为测试服 IP）
VITE_API_PROXY_TARGET=http://106.54.22.84:8889
